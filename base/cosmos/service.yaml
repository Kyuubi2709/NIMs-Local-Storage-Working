apiVersion: v1
kind: Service
metadata:
  name: nim-cosmos-service
spec:
  selector:
    deploymentInformation: DEPLOYMENT_INFORMATION # Placeholder to be replaced
  ports: []  # No ports defined here
